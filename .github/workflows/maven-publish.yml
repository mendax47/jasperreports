# This workflow will build a package using Maven and then publish it to GitHub packages when a release is created
name: Maven Package

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        server-id: github # Must match the <id> in your pom.xml distributionManagement
        settings-path: ${{ github.workspace }} # Generates settings.xml here

    - name: Build and Publish to GitHub Packages
      # 'deploy' handles the packaging automatically. Skipping tests prevents CI-specific graphic/font errors.
      run: mvn -B deploy -DskipTests -s $GITHUB_WORKSPACE/settings.xml
      env:
        GITHUB_TOKEN: ${{ github.token }}
